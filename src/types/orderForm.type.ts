export type OrderFormType = {
  name: string | null,
  last_name: string | null,
  phone: string | null,
  country: string | null,
  zip: string | null,
  address: string | null,
  product: string | null,
  comment: string | null,
}
